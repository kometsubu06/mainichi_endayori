<div class="min-h-screen bg-slate-50 flex items-center justify-center px-4">
  <div class="w-full max-w-md">
    <div class="text-center mb-6">
      <h1 class="text-2xl font-semibold tracking-tight text-slate-900">招待コードで登録</h1>
      <p class="text-slate-500 text-sm mt-1">園から共有されたコードを入力してください</p>
    </div>

    <div class="bg-white rounded-2xl shadow-sm border border-slate-200 p-6">
      <%= form_with url: invite_path, scope: :user, method: :post, class: "space-y-4", data: { turbo: false } do |f| %>
        <div>
          <label class="block text-sm font-medium text-slate-700">招待コード</label>
          <input type="text" name="invite_code" required
                 class="mt-1 block w-full rounded-lg border-slate-300 shadow-sm focus:border-sky-500 focus:ring-sky-500">
        </div>

        <div>
          <%= f.label :name, "氏名", class: "block text-sm font-medium text-slate-700" %>
          <%= f.text_field :name, required: true,
                class: "mt-1 block w-full rounded-lg border-slate-300 shadow-sm focus:border-sky-500 focus:ring-sky-500" %>
        </div>

        <div>
          <%= f.label :email, "メールアドレス", class: "block text-sm font-medium text-slate-700" %>
          <%= f.email_field :email, required: true, autocomplete: "email",
                class: "mt-1 block w-full rounded-lg border-slate-300 shadow-sm focus:border-sky-500 focus:ring-sky-500" %>
        </div>

        <div>
          <%= f.label :password, "パスワード", class: "block text-sm font-medium text-slate-700" %>
          <%= f.password_field :password, required: true, autocomplete: "new-password",
                class: "mt-1 block w-full rounded-lg border-slate-300 shadow-sm focus:border-sky-500 focus:ring-sky-500" %>
        </div>

        <%= f.submit "登録してはじめる",
              class: "w-full inline-flex justify-center items-center rounded-lg bg-sky-600 px-4 py-2.5 text-white font-semibold hover:bg-sky-700 transition" %>
      <% end %>
    </div>

    <div class="text-center mt-4 text-sm">
      すでにアカウントをお持ちですか？
      <%= link_to "ログイン", new_user_session_path, class: "text-sky-700 hover:text-sky-900" %>
    </div>
  </div>
</div>